{
  "name": "inkzlab-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "scripts": {
    "dev": "vite",
    "backend": "cd backend && nodemon server.js",
    "start": "concurrently \"npm run dev\" \"npm run backend\"",
    "build": "vite build"
  },

  "engines": {
    "node": "24.x"
  },

  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.14.1"
  },

  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.0",
    "vite": "^5.0.0"
  }
}